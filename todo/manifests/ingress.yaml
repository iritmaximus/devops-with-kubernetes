apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: dwk-todo-ingress
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: todo-svc
            port:
              number: 2345
